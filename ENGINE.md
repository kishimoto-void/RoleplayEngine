# ENGINE

この文書は α ではない。Hash-A に封入しない。

RoleplayEngine は Capsule の換装先ではない。
BOX の上の実行系である。

```
Capsule   何であるか
BOX       今どう扱うか
Runtime   今どの経路で通すか
Engine    物語の現象をイベントとして通すか
```

Ζ は min3 核に上げない。
生成文と Hash-A の照合 Gate は足さない。
仮組みは状態にしない。
世界事実は authorize された閉じた packet だけ。

需要面の置き場:

```
status   見える世界。制御文ではない
save     Scene / Ζ / 事件 / Hash-B。Hash-A は参照
tick     ユーザー沈黙でも次の現象
enter    exits 以外は拒否
VOICE    演算器の在庫。核ではない
SceneBook 用意された場面。発言では増えない
prepare  住所の換装。jump=False は links / exits だけ

```

